1、用Maven创建java项目。
mvn archetype:generate -DgroupId=echo.viedo.unit8 -DartifactId=javaVideo 
-DarchetypeArtifactId=maven-archetype-quickstart -DinteractiveMode=false

2、重构Unit4_Uint8的代码，将main,test部分分开

3、注意包名不能大写，不能有下划线等。